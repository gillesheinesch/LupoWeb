import React, { Component } from 'react';
import { Button } from 'rsuite';

export default class TopNavbar extends Component {

    render() {
        return (
          <Button appearance="primary"><i class="fab fa-discord"></i> Login</Button>
        )
    }
}
